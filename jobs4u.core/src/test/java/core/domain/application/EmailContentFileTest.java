package core.domain.application;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;
/*
class EmailContentFileTest {

    @Test
    void testValidContent() {
        String content = "This is a test email content.";
        EmailContentFile emailContentFile = new EmailContentFile(content);
        assertEquals(content, emailContentFile.toString());
    }

    @Test
    void testNullContent() {
        assertThrows(IllegalArgumentException.class, () -> new EmailContentFile(null));
    }

    @Test
    void testEmptyContent() {
        assertThrows(IllegalArgumentException.class, () -> new EmailContentFile(""));
    }

    @Test
    void testEquals() {
        String content1 = "Content";
        String content2 = "Content";
        EmailContentFile file1 = new EmailContentFile(content1);
        EmailContentFile file2 = new EmailContentFile(content2);
        assertTrue(file1.equals(file2));
    }

    @Test
    void testNotEquals() {
        String content1 = "Content1";
        String content2 = "Content2";
        EmailContentFile file1 = new EmailContentFile(content1);
        EmailContentFile file2 = new EmailContentFile(content2);
        assertFalse(file1.equals(file2));
    }

    @Test
    void testHashCode() {
        String content = "Content";
        EmailContentFile emailContentFile = new EmailContentFile(content);
        assertEquals(content.hashCode(), emailContentFile.hashCode());
    }
}

 */
