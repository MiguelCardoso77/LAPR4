package bootstrappers.bootstraping;

/*
import core.application.controllers;
import core.persistence.PersistenceContext;
import core.repositories.JobInterviewRepository;
import eapli.framework.actions.Action;

public class JobInterviewsBootstrapper implements Action {
    final JobInterviewRepository jobInterviewRepository = PersistenceContext.repositories().jobInterviews();

    final AddJobInterviewController controller = new AddJobInterviewController();

    @Override
    public boolean execute() {


        return true;
    }

    private void registerJobInterview(){
        controller.
    }
}

 */
