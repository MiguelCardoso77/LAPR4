package core.domain.application;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
/*
class FilesAttachedContentTest {

    @Test
    void testFilesAttachedContentEquality() {
        FilesAttachedContent content1 = FilesAttachedContent.valueOf("file1.txt");
        FilesAttachedContent content2 = FilesAttachedContent.valueOf("file1.txt");
        FilesAttachedContent content3 = FilesAttachedContent.valueOf("file2.txt");

        assertEquals(content1, content2); // Should be equal
        assertNotEquals(content1, content3); // Should not be equal
    }

    @Test
    void testFilesAttachedContentHashCode() {
        FilesAttachedContent content1 = FilesAttachedContent.valueOf("file1.txt");
        FilesAttachedContent content2 = FilesAttachedContent.valueOf("file1.txt");

        assertEquals(content1.hashCode(), content2.hashCode());
    }

    @Test
    void testFilesAttachedContentToString() {
        FilesAttachedContent content = FilesAttachedContent.valueOf("file1.txt");
        assertEquals("file1.txt", content.toString());
    }

    @Test
    void testInvalidFilesAttachedContent() {
        assertThrows(IllegalArgumentException.class, () -> {
            FilesAttachedContent.valueOf(null);
        });

        assertThrows(IllegalArgumentException.class, () -> {
            FilesAttachedContent.valueOf("");
        });
    }
}

 */
